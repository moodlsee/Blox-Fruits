local success, OrionLib = pcall(function()
    return loadstring(game:HttpGet("https://raw.githubusercontent.com/moodlsee/Blox-Fruits/refs/heads/main/Blox%20Fruit.txt"))()
end)

if not success then
    warn("\n[Erreur] Échec du chargement du script Blox Fruits\n")
    return
end

print("\n[Succès] Script chargé correctement !\n")

local Window = OrionLib:MakeWindow({
    Name = "Blox Fruits Script",
    HidePremium = false,
    SaveConfig = true,
    ConfigFolder = "BloxFruitsConfig",
    IntroEnabled = true,
    IntroText = "Welcome to Blox Fruits Script!"
})

local MainTab = Window:MakeTab({
    Name = "Main",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

MainTab:AddButton({
    Name = "Auto Farm XP",
    Callback = function()
        print("Auto Farm XP activated!")
        -- Ajoutez votre code d'auto farm ici
    end
})

MainTab:AddButton({
    Name = "Auto Collect Chests",
    Callback = function()
        print("Auto Collect Chests activated!")
        -- Ajoutez votre code de collecte de coffres ici
    end
})

MainTab:AddToggle({
    Name = "God Mode",
    Default = false,
    Callback = function(Value)
        print("God Mode: " .. tostring(Value))
        -- Ajoutez votre code pour activer/désactiver le God Mode ici
    end
})

OrionLib:Init()